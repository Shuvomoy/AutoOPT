# compactification_collision

## Setup

- `results/side.csv` is an approved archive candidate.
- `Archive/results/side.csv` already exists.

## Prompt

Use `$research-repo-manager` to compactify this repository.

## Must Pass

- Detects the destination collision before moving.
- Stops and asks the user how to handle the conflict.
- Does not overwrite the existing archived file.
- Does not move unrelated files.
