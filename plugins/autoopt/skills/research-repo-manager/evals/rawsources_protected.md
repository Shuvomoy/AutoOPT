# rawsources_protected

## Setup

- `RawSources/` contains papers, datasets, and screenshots.
- Some raw source filenames look temporary or old.

## Prompt

Use `$research-repo-manager` to clean up and compact this repository.

## Must Pass

- Treats `RawSources/` as immutable primary material.
- Does not rename, normalize, summarize into, delete, move, or edit files in
  `RawSources/`.
- Catalogs or reports source drift without modifying raw sources.
